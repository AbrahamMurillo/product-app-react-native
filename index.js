/**
 * @format
 */

import {AppRegistry} from 'react-native';
import ProductsApp from './src/Products';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => ProductsApp);
